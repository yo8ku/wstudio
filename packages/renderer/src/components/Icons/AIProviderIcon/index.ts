/**
 * AIProviderIcon 组件导出文件
 */

import { AIProviderIcon, AIProviderIconFromModel, getProviderFromModel } from './AIProviderIcon';

export { AIProviderIcon, AIProviderIconFromModel, getProviderFromModel };

