/**
 * Compatibility re-export for existing sidebar imports.
 * The standalone ExtensionPanel component now owns the mock data source.
 */

export { MOCK_EXTENSION_PANEL_ITEMS as MOCK_LOCAL_EXTENSIONS } from '../../../ExtensionPanel';
