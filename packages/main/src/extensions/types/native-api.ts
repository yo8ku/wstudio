/**
 * 原生 API 类型定义
 * 替代 @note-studio/extension-api/src/native-api
 */

/**
 * 原生 API 接口
 */
export interface NativeAPI {
  // 这里可以添加原生 API 的类型定义
}

/**
 * 默认导出
 */
const nativeApi: NativeAPI = {};

export default nativeApi;








